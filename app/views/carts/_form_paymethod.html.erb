<%= form_for :cart, url: pay_cart_url, method: :post do |f| %>
    
    <%= f.label :payment_method %>
    <%= f.select :payment_method, Payment.get_paymentmethod %>

    <%= f.hidden_field :order_id, value: @order.id %>
    <%= f.submit "Pay" %>
<% end %>